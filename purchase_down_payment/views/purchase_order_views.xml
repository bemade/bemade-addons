<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.view.form.inherit.purchase.down.payment</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <!-- In Odoo 19.0 the stock `action_create_invoice` header button
                 was removed from the purchase.order form (it now lives only
                 in the list view). Append our wizard-launching variant to
                 the header so users keep a "Create Bill" entry point on
                 the form. -->
            <xpath expr="//header" position="inside">
                <button name="%(purchase_down_payment.purchase_order_advance_payment_action)d"
                        string="Create Bill"
                        invisible="state not in ('purchase', 'done') or invoice_status in ('no', 'invoiced')"
                        type="action"
                        data-hotkey="w"/>
            </xpath>
        </field>
    </record>
</odoo>
