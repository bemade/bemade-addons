<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- UC-VAL-01 AC2 — daily competency-certification expiry activities. -->
    <record id="cron_cbet_certification_expiry" model="ir.cron">
        <field name="name">CBET: certification expiry activities</field>
        <field name="model_id" ref="model_cbet_certification"/>
        <field name="state">code</field>
        <field name="code">model._cron_expiry_activities()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
