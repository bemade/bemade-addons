<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <!-- Template for creating a new injury via the portal -->
        <template id="portal_create_injury">
            <t t-call="portal.portal_layout">
                <t t-set="breadcrumbs_searchbar" t-value="True"/>
                <t t-call="portal.portal_searchbar">
                    <t t-set="title">Report Injury</t>
                </t>
                <div class="container pt-3">
                    <div class="row mt-3">
                        <div class="col-lg-12">
                            <h2><span t-if="is_treatment_prof">Add Injury for</span><span t-else="">Report Injury for</span> <t t-esc="patient.name"/></h2>
                            
                            <!-- Task 1412: the form body is a sub-template, shared with the
                                 clinic dossier's quick-add modal (rendered alone, modal=quick=True). -->
                            <t t-call="bemade_sports_clinic.portal_injury_create_form_body"/>
                        </div>
                    </div>
                </div>
            </t>
        </template>
        

        <!-- Task 1412: the create form's body — t-called by portal_create_injury
             (page) and rendered ALONE by /my/patient/injury/new/fragment for the
             clinic dossier's quick-add modal. Values as the page, plus:
               modal        — True ⇒ no page action rows, a Close/Save footer,
                              no DOMContentLoaded script (the modal JS inits);
               quick        — True ⇒ the reduced field set (no TP checklist, no
                              parental consent, a Status select);
               quick_team_id / quick_tp_id / stages / quick_stage — the
                              quick-add pre-fills (see the controller). -->
        <template id="portal_injury_create_form_body" name="Injury Create Form Body">
            <form action="/my/patient/injury/create" method="post" enctype="multipart/form-data"
                  t-att-class="'o_sc_injury_modal_form' if modal else None">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                <input type="hidden" name="patient_id" t-att-value="patient.id"/>
                <input type="hidden" name="return_url" t-att-value="return_url"/>
                <input t-if="team_context_id" type="hidden" name="team_context_id" t-att-value="team_context_id"/>
                <!-- Clinic nav context (task 1410): survives the create round-trip -->
                <input t-if="clinic_event" type="hidden" name="clinic_id" t-att-value="clinic_event.id"/>
                <!-- Quick-add (task 1412, clinic modal): the current user is the
                     injury's treatment professional, assigned but not shown.
                     ONE isolated block — #1240 will drop it with the field. -->
                <input t-if="quick and quick_tp_id" type="hidden" name="treatment_professional_ids[]" t-att-value="quick_tp_id"/>
                <!-- Error messages -->
                <div t-if="error" class="alert alert-danger">
                    <t t-if="error == 'team_required'">Please select a team for this injury.</t>
                    <t t-elif="error == 'invalid_team'">The selected team is not valid for this player.</t>
                    <t t-else="">An error occurred. Please review the form.</t>
                </div>
                
                <!-- Top Action Buttons (page only; the modal has its footer) -->
                <div t-if="not modal" class="row mb-4">
                    <div class="col-lg-12">
                        <div class="d-flex justify-content-between">
                            <a t-att-href="return_url" class="btn bg-o-color-2 text-white">
                                <i class="fa fa-times"></i> Cancel
                            </a>
                            <button type="submit" class="btn bg-o-color-1 text-white">
                                <i class="fa fa-save"></i> <span t-if="is_treatment_prof">Create Injury</span><span t-else="">Submit Report</span>
                            </button>
                        </div>
                    </div>
                </div>
                
                <!-- Injury Details Section -->
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        <h4 class="mb-0">Injury Details</h4>
                    </div>
                    <div class="card-body">
                        <!-- Team selection. Quick-add (task 1412): ONE hidden input when
                             the team is unambiguous (the clinic's single team, or the
                             patient's only team), else this same select — #1240 will
                             drop the block with the field. -->
                        <input t-if="quick and quick_team_id" type="hidden" name="team_id" t-att-value="quick_team_id"/>
                        <div t-if="not (quick and quick_team_id)" class="row">
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label for="team_id">Team</label>
                                    <select name="team_id" id="team_id" class="form-control"
                                            t-att-required="require_team_selection and 'required' or False">
                                        <option value="" t-if="require_team_selection">-- Select team --</option>
                                        <t t-foreach="patient_teams" t-as="t">
                                            <option t-att-value="t.id" t-att-selected="selected_team_id == t.id and 'selected' or False">
                                                <t t-esc="t.name"/>
                                            </option>
                                        </t>
                                    </select>
                                    <small t-if="not quick" class="text-muted">The selected team determines assigned therapists.</small>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Basic injury information -->
                        <div class="row">
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label for="injury_date">Date of Injury</label>
                                    <div class="d-flex align-items-center">
                                        <input type="date" name="injury_date" id="injury_date" class="form-control" 
                                               t-att-required="'injury_date_na' not in request.params"/>
                                        <div class="form-check ml-3">
                                            <input type="checkbox" name="injury_date_na" id="injury_date_na" class="form-check-input"
                                                   t-att-checked="'injury_date_na' in request.params"
                                                   onchange="(function(){var el=document.getElementById('injury_date'); if(!el){return;} el.required=!this.checked; if(this.checked){ el.value=''; el.disabled=true; el.setAttribute('disabled','disabled'); } else { el.disabled=false; el.removeAttribute('disabled'); } }).call(this)"/>
                                            <label for="injury_date_na" class="form-check-label">N/A</label>
                                        </div>
                                    </div>
                                    <small class="text-muted">Check N/A if exact date is unknown</small>
                                    <script t-if="not modal">
                                        <![CDATA[
                                        // Initialize required/disabled state based on N/A checkbox
                                        document.addEventListener('DOMContentLoaded', function() {
                                            const naCheckbox = document.getElementById('injury_date_na');
                                            const dateInput = document.getElementById('injury_date');
                                            if (naCheckbox && dateInput) {
                                                dateInput.required = !naCheckbox.checked;
                                                if (naCheckbox.checked) {
                                                    dateInput.disabled = true;
                                                    dateInput.setAttribute('disabled','disabled');
                                                } else {
                                                    dateInput.disabled = false;
                                                    dateInput.removeAttribute('disabled');
                                                }
                                            }
                                        });
                                        ]]>
                                    </script>
                                </div>
                            </div>
                            <div t-if="is_treatment_prof" class="col-lg-6">
                                <div class="form-group">
                                    <label for="predicted_resolution_date">Predicted Resolution Date</label>
                                    <input type="date" name="predicted_resolution_date" id="predicted_resolution_date" class="form-control"/>
                                    <small class="text-muted">Expected recovery timeline</small>
                                </div>
                            </div>
                        </div>

                        <!-- Status (quick-add only, task 1412; TPs — default Active) -->
                        <div t-if="quick and is_treatment_prof and stages" class="row mt-3">
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label for="stage">Status</label>
                                    <select name="stage" id="stage" class="form-control">
                                        <t t-foreach="stages" t-as="stage">
                                            <option t-att-value="stage[0]" t-att-selected="'selected' if stage[0] == quick_stage else None">
                                                <t t-esc="stage[1]"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Diagnosis -->
                        <div class="row mt-3">
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label for="diagnosis">Diagnosis/Description</label>
                                    <textarea name="diagnosis" id="diagnosis" class="form-control" 
                                        rows="3" required="required"></textarea>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Treatment professionals and parental consent (only for treatment
                             professionals; full page only — the quick-add assigns the current
                             user silently and leaves consent to the full form, task 1412) -->
                        <div t-if="is_treatment_prof and not quick" class="row mt-3">
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label class="form-label">Treatment Professionals</label>
                                    <div class="border rounded p-3" style="max-height: 200px; overflow-y: auto;">
                                        <t t-foreach="treatment_professionals" t-as="tp">
                                            <div class="form-check">
                                                <input class="form-check-input" type="checkbox" 
                                                       t-att-id="'tp_%s' % tp.id"
                                                       name="treatment_professional_ids[]" 
                                                       t-att-value="tp.id"/>
                                                <label class="form-check-label" t-att-for="'tp_%s' % tp.id">
                                                    <span t-esc="tp.name"/>
                                                    <small class="text-muted d-block" t-esc="tp.email"/>
                                                </label>
                                            </div>
                                        </t>
                                    </div>
                                    <small class="text-muted">Select one or more professionals to assign. If left blank, team therapists will be auto-assigned. You will not be auto-assigned unless selected.</small>
                                </div>
                            </div>
                            <div class="col-lg-6">
                                <div class="form-group">
                                    <label for="parental_consent">Parental Consent</label>
                                    <select name="parental_consent" id="parental_consent" class="form-control" required="required">
                                        <t t-foreach="parental_consent_options" t-as="option">
                                            <option t-att-value="option[0]">
                                                <t t-esc="option[1]"/>
                                            </option>
                                        </t>
                                    </select>
                                </div>
                            </div>
                        </div>
                        
                        <!-- Internal notes (only visible to treatment professionals) -->
                        <div t-if="is_treatment_prof" class="row mt-3">
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label for="internal_notes">Internal Notes (Medical Staff Only)</label>
                                    <textarea name="internal_notes" id="internal_notes" class="form-control"
                                        rows="3"></textarea>
                                    <small class="text-muted">Notes visible only to treatment professionals</small>
                                </div>
                            </div>
                        </div>

                        <!-- Hide-from-coaches toggle (TPs only) -->
                        <div t-if="is_treatment_prof" class="row mt-2">
                            <div class="col-lg-12">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox"
                                           name="hidden_from_coaches" id="hidden_from_coaches" value="1"/>
                                    <label class="form-check-label" for="hidden_from_coaches">
                                        Hide from coaches
                                    </label>
                                    <small class="d-block text-muted">When checked, team coaches will not see this injury anywhere in the portal.</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- External Notes Section -->
                <div class="card mb-4">
                    <div class="card-header bg-secondary text-white">
                        <h4 class="mb-0">External Notes</h4>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="form-group">
                                    <label for="external_notes">External Notes</label>
                                    <textarea name="external_notes" id="external_notes" class="form-control" 
                                        rows="4"></textarea>
                                    <small class="text-muted">Notes visible to all team members</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Modal footer (task 1412): Close + Save -->
                <div t-if="modal" class="d-flex justify-content-end gap-2 mt-3">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="submit" class="btn bg-o-color-1 text-white">
                        <i class="fa fa-save me-1" aria-hidden="true"/>Save
                    </button>
                </div>

                <!-- Action buttons (page only) -->
                <div t-if="not modal" class="row mt-4">
                    <div class="col-lg-12">
                        <div class="float-left">
                            <a t-att-href="return_url" class="btn bg-o-color-2 text-white">Cancel</a>
                            
                            <!-- Submit button -->
                            <button type="submit" class="btn bg-o-color-1 text-white">
                                <span t-if="is_treatment_prof">Create Injury</span><span t-else="">Submit Report</span>
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </template>

        <!-- Report Injury button now integrated directly into the portal_my_player_injuries template -->
        
        <!-- Success confirmation page -->
        <template id="portal_injury_created">
            <t t-call="portal.portal_layout">
                <div class="container mt-3">
                    <div class="alert alert-success">
                        <h4>Success!</h4>
                        <p>The injury has been reported successfully.</p>
                    </div>
                    <div class="mt-3">
                        <a t-att-href="return_url" class="btn bg-o-color-1 text-white">Return</a>
                    </div>
                </div>
            </t>
        </template>
    </data>
</odoo>
